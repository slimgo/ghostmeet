# 02 — Панель проверки закрывается

**Status:** done

Повторное нажатие убирает результат, а сам он уходит через полминуты. Панель
описывает момент, а не состояние: список, который висит до утра, будет прочитан как
правда про утро.

**Это была невыполненная строка закрытого тикета** (`connection-check/03-ui`,
«результат не остаётся на экране навсегда»). Тикет закрывался по работающей кнопке,
а не по своим же критериям.

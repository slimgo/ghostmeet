# 02 — Зависание на «Сохранить диалог»

**Status:** needs-info

Тот же прогон 0.7.3: после того как `Them` замолчал, нажатие «Сохранить диалог»
подвесило приложение надолго, и файл не появился. Путь сохранения чист — имя,
экспорт, модальная `NSSavePanel`, ничего нового с 0.3.0, — значит главный поток
был занят чем-то ещё **до** нажатия, и чем именно, без журнала не сказать.
Перечисление устройств (2 мс) и поток автоотката исключены чтением кода.

Нужен журнал того прогона; команда владельцу уже дана:

```bash
log show --predicate 'process == "GhostMeet"' --last 1h --style compact > /Volumes/DockStantion/MyProjects2/AI/GhostMeet-log.txt
```

Смотреть строки `ЗАХВАТ`, `КАНАЛ THEM`, `КАНАЛ YOU`, `МИКРОФОН`, `РАСПОЗНАВАНИЕ`,
`МАРШРУТ`, а также `error` и `fault` вокруг момента нажатия.

Гипотеза, не более: зависание — следствие тикета 01. Сторожа ещё не было, поток
`Them` числился живым, и что-то на главном потоке могло ждать его до упора.
Проверяется тем же сценарием на 0.7.4: если со сторожем зависание не повторится,
гипотеза подтверждена; если повторится — это отдельная поломка и отдельный тикет.
